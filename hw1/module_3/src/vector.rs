use std::fs::File;
use std::io::Error;
use std::io::{BufReader, Read, Write};

pub fn serialize_vector_to_disk(data: Vec<i32>, filename: &str) -> Result<(), Error> {
    panic!("TODO: Complete this Code Segment");
}

pub fn deserialize_vector_from_disk(filename: &str) -> Vec<i32> {
    panic!("TODO: Complete this Code Segment");
}
